{% extends "base.html.twig" %}

{% block body %}
    <h1>{{ category.name | title }}</h1>

    <div class="row">
        {% for subcategory in subcategories %}
            <div class="col-12">
                <a href="{{ url('category.subcategory', { categorySlug: category.slug, subcategorySlug: subcategory.slug } ) }}"
                   class="text-light h4">
                    {{ subcategory.name | title }}
                    <svg width="1em" height="1em" viewBox="0 0 16 16" class="bi bi-chevron-right" fill="currentColor"
                         xmlns="http://www.w3.org/2000/svg">
                        <path fill-rule="evenodd"
                              d="M4.646 1.646a.5.5 0 0 1 .708 0l6 6a.5.5 0 0 1 0 .708l-6 6a.5.5 0 0 1-.708-.708L10.293 8 4.646 2.354a.5.5 0 0 1 0-.708z"/>
                    </svg>
                </a>

                <div class="row">
                    {% for gif in get_gif() | filter(value => value.category == subcategory) %}
                        <div style="margin: 10px">
                            <a href="{{ url('gif.index', { gifSlug: gif.Slug }) }}">
                                <img style="width: 140px; height: 140px" src="{{ asset('img/' ~ gif.source) }}" alt=""
                                     class="img-fluid">
                            </a>
                        </div>
                        <div style="margin: 10px">
                            <a href="{{ url('gif.index', { gifSlug: gif.Slug }) }}">
                                <img style="width: 140px; height: 140px" src="{{ asset('img/' ~ gif.source) }}" alt=""
                                     class="img-fluid">
                            </a>
                        </div>
                        <div style="margin: 10px">
                            <a href="{{ url('gif.index', { gifSlug: gif.Slug }) }}">
                                <img style="width: 140px; height: 140px" src="{{ asset('img/' ~ gif.source) }}" alt=""
                                     class="img-fluid">
                            </a>
                        </div>
                        <div style="margin: 10px">
                            <a href="{{ url('gif.index', { gifSlug: gif.Slug }) }}">
                                <img style="width: 140px; height: 140px" src="{{ asset('img/' ~ gif.source) }}" alt=""
                                     class="img-fluid">
                            </a>
                        </div>
                    {% endfor %}
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}